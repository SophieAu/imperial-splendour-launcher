name: Build

on:
  push:
    branches: [ master ]

jobs:

  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.16
    
    - name: Install Wails
      run: go get -u github.com/wailsapp/wails/cmd/wails@v1.16.2
    
    - name: Build Launcher
      run: |
        wails build -d
        mv build/imperial-splendour-launcher.exe build/imperial-splendour-launcher_debug.exe
        wails build -p

    - name: Extract Artifacts
      uses: actions/upload-artifact@v2
      with:
        name: executables
        path: build/
          
