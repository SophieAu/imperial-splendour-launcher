name: Temp

on:
  push:
    branches: [ master ]

jobs:
  temp-check-inno:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
 
    - name: Test Launcher Backend
      run: |
        echo "derp"
        iscc /HELP
        echo "mrp"

